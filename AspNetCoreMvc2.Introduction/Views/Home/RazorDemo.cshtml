@model AspNetCoreMvc2.Introduction.Model.emloyeeListViewModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>RazorDemo</title>
</head>
<body>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>City Id</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Employees)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.CityId</td>
                </tr>
            }
        </tbody>
    </table>

    Mail me : enestilici@gmail.com
    I am @@home

    <p>@DateTime.Now</p>
    @{ var date = DateTime.Now - TimeSpan.FromDays(3);}
    <p>3 days ago was @date</p>

    @{ var number = 10;}
    <p>@number</p>

    @if (number < 10)
    {
        <p> Your number is less than 10</p>
    }
    else if (number == 10)
    {
        <p>your number is 10</p>
    }
    else
    {
        <p>Your number is greater than 10</p>
    }

    @for (int i = 0; i < Model.Employees.Count; i++)
    {
        <br /> @Model.Employees[i].FirstName
    }

    @try
    {

    }
    catch (ArgumentNullException argumentNullException)
    {

        throw;
    }
    catch (Exception exception)
    {
        throw;
    }
    finally
    {

    }

    @using (Html.BeginForm())
    {
        <input type="text" name="name" value="" />
        <button type="submit"></button>
    }
</body>
</html>
